pre-commit:
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: pnpm biome check --write {staged_files}
      stage_fixed: true
pre-push:
  commands:
    test-unit:
      run: pnpm run test
    test-e2e:
      run: pnpm run test:e2e